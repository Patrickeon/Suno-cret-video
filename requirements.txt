# ────────────────────────────────────────────────────────────────
# 기본 기능(영상/자막/썸네일/쇼츠)은 pip 패키지가 전혀 필요 없습니다.
# Python 표준 라이브러리 + ffmpeg 만으로 동작합니다.
#
# 아래 stable-ts 는 "--align auto" (가사 자동 강제정렬) 에만 필요합니다.
# 이 기능을 쓸 때만 설치하세요:  pip install -r requirements.txt
#
# stable-ts 는 torch(~2GB)를 함께 받습니다. GPU가 없으면 먼저 CPU 전용 torch 를
# 받으면 가볍습니다:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# ────────────────────────────────────────────────────────────────

stable-ts

# ────────────────────────────────────────────────────────────────
# 아래 Pillow 는 "--disc-theme text_ring" (레코드 모드의 회전 텍스트 링 테마)
# 에만 필요합니다. 커브드(곡선) 텍스트는 글자 단위 회전 배치가 필요해 순수
# ffmpeg 필터로는 만들기 어렵기 때문입니다. classic/lp_vinyl 테마를 포함한
# 나머지 모든 기본 기능은 Pillow 없이 그대로 동작합니다.
# 이 테마를 쓸 때만 설치하세요:
#   pip install Pillow
# ────────────────────────────────────────────────────────────────

Pillow
